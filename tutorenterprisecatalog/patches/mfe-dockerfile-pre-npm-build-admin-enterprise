ENV LMS_BASE_URL='{% if LMS_BASE_URL is defined %}{{ LMS_BASE_URL }}{% else %}{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ LMS_HOST }}{% endif %}'
# env.config.jsx is useful only for the applications using the frontend-plugin-framework.
# Our custom branch isn't using it. So let's remove it to avoid missing-dependency issues during build time.
RUN rm /openedx/app/env.config.jsx
