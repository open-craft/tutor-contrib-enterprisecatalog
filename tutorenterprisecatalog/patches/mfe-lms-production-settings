MFE_CONFIG["ENTERPRISE_CATALOG_API_BASE_URL"] = "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ ENTERPRISE_CATALOG_HOST }}"
MFE_CONFIG["DISCOVERY_API_BASE_URL"] = "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ DISCOVERY_HOST }}"
MFE_CONFIG["ECOMMERCE_BASE_URL"] = "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ ECOMMERCE_HOST }}"
MFE_CONFIG["LMS_BASE_URL"] = "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ LMS_HOST }}"
