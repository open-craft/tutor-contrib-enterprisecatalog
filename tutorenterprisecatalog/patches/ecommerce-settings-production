CORS_ORIGIN_WHITELIST = list(CORS_ORIGIN_WHITELIST)
CORS_ORIGIN_WHITELIST.append("https://{{ LMS_HOST }}")
CSRF_TRUSTED_ORIGINS = list(CSRF_TRUSTED_ORIGINS)
CSRF_TRUSTED_ORIGINS.append("https://{{ LMS_HOST }}")
